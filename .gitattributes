# Normalise line endings. run_pipeline.sh MUST be LF: a CRLF shebang makes
# bash fail with "$'\r': command not found". This replaces the sed workaround
# at gcp_run.ps1:478.
* text=auto

*.sh   text eol=lf
*.py   text eol=lf
*.toml text eol=lf
*.md   text eol=lf

*.ps1  text eol=crlf
